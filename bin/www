#!/usr/bin/env node
const winston = require('winston');
const config = require('../config');
const app = require('../app');


app.listen(config.app.PORT,
  _ => winston.info(`Server now running at port ${config.app.PORT}`));
